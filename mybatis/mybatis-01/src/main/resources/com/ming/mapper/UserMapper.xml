<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--命名空间=绑定一个对象的Dao、Mapper接口-->
<mapper namespace="com.ming.mapper.UserMapper">
    <update id="updateUser" parameterType="com.ming.pojo.User">
        update user set name = #{name}, pwd = #{pwd} where id = #{id};
    </update>
    
    <delete id="deleteUserById" parameterType="int">
        delete from user where id = #{id}
    </delete>
    <delete id="deleteUserByIds" parameterType="list">
        delete from user where id in
        <foreach collection="list" item="id" separator="," open="(" close=")">#{id}</foreach>
    </delete>
    <select id="getUserList" resultType="com.ming.pojo.User">
    select * from user
    </select>

    <select id="getUserById" resultType="com.ming.pojo.User" parameterType="int">
        select * from user where id = #{id}
    </select>

    <select id="getUserById2" resultType="com.ming.pojo.User" parameterType="map">
        select * from user where id = #{id} and name = #{name}
    </select>

    <select id="getUserLike" resultType="com.ming.pojo.User">
        select * from mybatis.user where name like #{value}
    </select>
    <!--对象中的属性可以直接读取出来-->
    <insert id="addUser" parameterType="com.ming.pojo.User" >
        insert into user (id, name, pwd) values (#{id},#{name},#{pwd})
    </insert>

    <!--// 万能map
    对象中的属性，
    可以直接取出来传递map的key-->
    <insert id="addUser2" parameterType="map" >
        insert into user (id, name, pwd) values (#{userId},#{userName},#{password})
    </insert>

</mapper>