<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<!--核心配置文件-->
<configuration>
    <!--引入外部配置文件-->
    <properties resource="db.properties">
<!--        &lt;!&ndash;优先读取配置文件&ndash;&gt;
        <property name="username" value="root"/>
        <property name="password" value="123456"/>-->
    </properties>
    
    <settings>
        <!--标准的日志生成-->
        <setting name="logImpl" value="STDOUT_LOGGING"/>
        <!--log4j 日志-->
<!--        <setting name="logImpl" value="LOG4J"/>-->
    </settings>

    <!--可以给实体类取别名-->
<!--    <typeAliases>
        <package name="com.ming.pojo.User"/>
    </typeAliases>-->

    <typeAliases>
        <typeAlias type="com.ming.pojo.User" alias="User"></typeAlias>
    </typeAliases>

    <environments default="development">
        <environment id="development">
            <!--事务管理器-->
            <transactionManager type="JDBC"/>
            <!--连接池-->
            <dataSource type="POOLED">
                <property name="driver" value="${driver}"/>
                <property name="url" value="${url}"/>
                <property name="username" value="${username}"/>
                <property name="password" value="${password}"/>
            </dataSource>
        </environment>

    </environments>

    <!--绑定接口-->
    <mappers>
        <mapper class="com.ming.mapper.UserMapper"></mapper>
        <!--通配-->
        <mapper resource="com/ming/mapper/*Mapper.xml"></mapper>
    </mappers>
</configuration>