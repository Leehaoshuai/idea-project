<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

    <!--使用Spring创建对象，在spring中这些都称为Bean

    类型 变量名 = new 类型();
    Hello hello = new Hello();

    bean = 对象 new Hello()
    id = 变量名
    class = new 的对象
    property 相当于给对象中的属性设置值
    控制：谁来控制对象的创建，传统应用程序的对象是由程序本身控制创建的，使用Spring后，对象是由Spring来创建的 beans.xml
    反转：程序本身不创建对象，而变成被动的接收对象 context.getBean()
            ApplicationContext context = new ClassPathXmlApplicationContext("beans.xml");
        // 我们的对象现在都在Spring中管理了，需要的时候取出来就行
        Hello hello = (Hello) context.getBean("hello");
    依赖注入: 就是利用set方法来进行注入的， 属性
    IOC是一种编程思想，由主动的编程变成被动的编程

    IOC 怎么创建的对象
    -->
    <bean id="hello" class="com.ming.pojo.Hello">
        <property name="str" value="Spring"/>
    </bean>

</beans>